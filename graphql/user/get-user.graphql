query GetUser($login: String!) {
  user(login: $login) {
    id
    login
    name
    bio
    company
    location
    websiteUrl
    avatarUrl
    createdAt
    followers {
      totalCount
    }
    following {
      totalCount
    }
    repositories {
      totalCount
    }
  }
}
